-- Create users table
CREATE TABLE users (
    user_id INT PRIMARY KEY,
    name TEXT,
    signup_date DATE
);

-- Create orders table
CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    user_id INT,
    amount INT
);

-- Create activity table
CREATE TABLE activity (
    user_id INT,
    login_count INT
);
-- Insert into users
INSERT INTO users (user_id, name, signup_date) VALUES
(1, 'Aarav', '2023-01-10'),
(2, 'Diya', '2023-02-14'),
(3, 'Rohan', '2023-03-01'),
(4, 'Meera', '2023-04-11'),
(5, 'Kabir', '2023-05-06');

-- Insert into orders
INSERT INTO orders (order_id, user_id, amount) VALUES
(101, 1, 500),
(102, 1, 700),
(103, 2, 300),
(104, 4, 900),
(105, 4, 400);

-- Insert into activity
INSERT INTO activity (user_id, login_count) VALUES
(1, 25),
(2, 10),
(5, 12);
-- 1. User name and order amount (INNER JOIN)
SELECT u.name, o.amount
FROM users u
INNER JOIN orders o ON u.user_id = o.user_id;

-- 2. User name and total order amount (SUM + INNER JOIN)
SELECT u.name, SUM(o.amount) AS total_order_amount
FROM users u
INNER JOIN orders o ON u.user_id = o.user_id
GROUP BY u.name;

-- 3. All users and login counts (LEFT JOIN)
SELECT u.name, a.login_count
FROM users u
LEFT JOIN activity a ON u.user_id = a.user_id;

-- 4. Users who never placed an order
SELECT u.name
FROM users u
LEFT JOIN orders o ON u.user_id = o.user_id
WHERE o.order_id IS NULL;

-- 5. Combine all three tables
SELECT u.user_id, u.name, COALESCE(SUM(o.amount),0) AS total_order_amount, a.login_count
FROM users u
LEFT JOIN orders o ON u.user_id = o.user_id
LEFT JOIN activity a ON u.user_id = a.user_id
GROUP BY u.user_id, u.name, a.login_count;

-- 6. Users who placed orders greater than 400
SELECT u.name, o.amount
FROM users u
INNER JOIN orders o ON u.user_id = o.user_id
WHERE o.amount > 400;

-- 7. Top 3 users by total order amount
SELECT u.name, SUM(o.amount) AS total_order_amount
FROM users u
INNER JOIN orders o ON u.user_id = o.user_id
GROUP BY u.name
ORDER BY total_order_amount DESC
LIMIT 3;

-- 8. Users sorted by total order amount DESC, then login count DESC
SELECT u.name, COALESCE(SUM(o.amount),0) AS total_order_amount, a.login_count
FROM users u
LEFT JOIN orders o ON u.user_id = o.user_id
LEFT JOIN activity a ON u.user_id = a.user_id
GROUP BY u.name, a.login_count
ORDER BY total_order_amount DESC, a.login_count DESC;

-- 9. Average order amount per user
SELECT u.name, AVG(o.amount) AS avg_order_amount
FROM users u
INNER JOIN orders o ON u.user_id = o.user_id
GROUP BY u.name;

-- 10. Combined query: top 3 users with login counts
SELECT u.user_id, u.name, COALESCE(SUM(o.amount),0) AS total_order_amount, a.login_count
FROM users u
LEFT JOIN orders o ON u.user_id = o.user_id
LEFT JOIN activity a ON u.user_id = a.user_id
GROUP BY u.user_id, u.name, a.login_count
ORDER BY total_order_amount DESC
LIMIT 3;